// """Write a function called addCommas, which accepts a number 
// and converts it into a string formatted with commas."""


function addCommas(number) {
    return number.toLocaleString();
}

module.exports = addCommas;